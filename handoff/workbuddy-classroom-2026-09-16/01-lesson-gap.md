# 课时缺口核对（2026-09-16）

结论：确实有大量课时尚未进入课堂，不是前端下拉框截断。前端直接渲染 `/classroom/lessons` 全部返回项；后端仅返回数据库中 `textbook/%`、`VERIFIED`、允许课堂使用、所属章节已发布的来源，按 `chapter_id + source_path` 分组。

原教材索引有 10 章、57 份课时文件；`curriculum.json` 进一步规划为 95 个细分课时。这是两个不同粒度，不能直接用 95 减 8 宣称缺了 87 门完整课程。当前私有导入清单只有 29 个已对照原 PDF 核验的页面，组成 8 个选段，分别来自以下 8 份原课时：

| 章节 | 原课时总数 | 当前可用选段来源 |
| --- | ---: | --- |
| 01 绪论 | 8 | 01-04、01-06 |
| 02 线性表 | 7 | 无 |
| 03 栈与队列 | 5 | 无 |
| 04 串 | 3 | 04-03 |
| 05 数组与广义表 | 5 | 05-03 |
| 06 树与二叉树 | 8 | 06-02、06-03、06-04 |
| 07 图 | 7 | 无 |
| 08 查找 | 7 | 08-01 |
| 09 内部排序 | 6 | 无 |
| 10 外部排序 | 1 | 无 |

以上 8 份课时也可能只有部分页面通过核验，不能视为整课完整覆盖。其余 49 份原课时尚无可用核验选段；第 02、03、07、09、10 章当前完全没有可用选段。原 OCR 审计含 362 个页面块，其中此前明确人工核验 29 页；未核验内容不能仅因文件存在就标记为 `VERIFIED`。

补全顺序：按原 PDF 逐页核对并记录页码、原文差异和审核状态 → 受控导入 → 核对数据库与 API 数量 → 真实选课备课验证。不要开启 `auto-publish-local` 把未核验 OCR 一次性标为已核验；不要拿 PPT、参考答案或模型常识冒充教材来源。
