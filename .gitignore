# === Secrets & Environment ===
.env
.env.*
!.env.example
!deployment/.env.spring.example
.jwt-secret
.structify-askpass.cmd

# === Database ===
data.db
data.db-shm
data.db-wal
*.db
*.db-*
*.sqlite
*.sqlite-*
*.sqlite3
*.sqlite3-*

# === Dependencies ===
node_modules/
backend/spring/target/
backend/node/.env
backend/node/.jwt-secret
backend/node/data.db
backend/node/data.db-shm
backend/node/data.db-wal

# === User Uploads ===
uploads/

# === Private Course Knowledge ===
# Full textbook OCR may be copyrighted and must not be pushed to the public repository.
private/
knowledge/private/
lesson-materials/
presentation-materials/
teach_ppt/
pdfs/

# === Local Build / Browser Artifacts ===
# Rendered PPT output and browser traces are local evidence, not release inputs.
frontend/dist/
output/
.playwright-cli/
codex-temp/
release-artifacts/
__pycache__/
*.py[cod]

# === Logs ===
*.log

# === Backup & Temp Files ===
*.bak
*.bak-*
*.backup-*
*.tmp-*
*.tar.gz
prototype.restore-*
prototype.wip-*

# === Screenshots & Debug Images ===
*.png
*.jpg
*.jpeg

# === Debug & Test Outputs ===
public-*.html
public-*.txt
public-*.json
public-*.headers.txt
online-page.tmp.html
local-root-*.html
chrome-*.html
tmp-*/
tmp-*.html
# Scratch probes used while verifying OCR/textbook alignment. They hard-code local paths and are
# throwaway by design; the durable scripts live in scripts/ and tools/.
tmp_*.py
tmp_*.mjs
tmp_*.txt
tmp-*.sql
audit-current-report.json

# === Server Config (contains IP/infra info) ===
update-server.sh
Caddyfile.remote

# === Local deployment answers and working notes ===
docs/production-input-checklist.md
docs/production-input-response-review.md
docs/private-resource-inventory.local.md
docs/superpowers/
task_plan.md
findings.md
progress.md

# === AI Tool Config ===
.superpowers/
.codegraph/
.workbuddy/

# === OS & Editor ===
.DS_Store
Thumbs.db
.vscode/
.idea/

# === Test Output ===
test-results/

# === Local canvas artifacts ===
*.miora

# Frame-capture audit: reads backend/dsvp/engine from the repo root, which the
# production image does not ship. Local probe only - keeping it out of the index
# keeps it out of the release pack too.
frontend/src/animation/engine-frames.spec.ts
