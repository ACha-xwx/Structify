# 给 Workbuddy 的完整任务书（请整体复制）

你现在是 Structify 项目课堂与教材覆盖工作的唯一执行者。用户不希望再收到分阶段提示词；请自主推进从核验、修订、实现、受控导入到本地验收的完整闭环。只有遇到缺少原始 PDF、数据真实性无法确认、生产环境操作或其它无法安全自行解决的实质阻塞时，才向用户提出一个明确问题。不要以阶段性计划替代执行，也不要把部分覆盖说成全书完成。

## 项目背景

- 正确仓库是 `F:\data-structure-agent\data-structure-agent\tmp\structify-repair`，对应 Structify（Vue 前端 + Spring 后端）。上两级另有旧 Node/HTML 项目，只能读取其中教材资料，不能把旧业务代码迁入或误改旧仓库。当前工作树已有大量未提交改动，必须保留，不重置、不覆盖。
- 最终计划部署到 `structify.cn`，但本任务只做本地实现与验收；不提交、不推送、不部署、不访问生产数据库。
- 登录、认证、服务器 API、Admin 必须保留。普通用户登录后进入有原背景的极简课堂：大字、少元素，仅课时选择、老师讲解、板书关键词、输入与当前操作；不要恢复旧侧栏、统计、小字或多余导航。
- 课堂设计：教材是事实来源；模型负责备课、自然讲解、插问回答和语义判答；程序负责权限、来源、协议、版本、持久化及动画算法状态。学生手动推进，插问优先且不吞主线，刷新后恢复；动画由本地确定性 DSVP 模拟器计算，模型不得编造帧。PPT 暂不接入。
- 当前前端主页面是 `frontend/src/classroom/ClassroomView.vue`；课时 API/备课为 `backend/spring/src/main/java/com/feng/dsagent/classroom/ClassroomPreparation.java`；导入链见 `backend/spring/src/main/java/com/feng/dsagent/knowledge/KnowledgeCorpusLoader.java`、`JdbcKnowledgeChunkRepository.java`、`docs/content-import-guide.md`。工作日志为 `docs/project/00-current-status.md`。
- 本机私有测试数据库、已核验教材、服务配置位于仓库 Git 忽略的 `private/`。可以按需在本机测试中使用已有 API Key 和测试账号，但绝不能输出、复制、提交密钥或密码，也不能把受版权保护的 PDF/教材正文放进 Git 跟踪文件。

## 已有证据与真实缺口

先完整阅读 `docs/structify-lesson-coverage-audit-2026-09-16.md`、本目录前三个文件、`docs/structify-repair-handoff-2026-09-15.md`，再用实际源码和数据重新核对。原教材索引位于 `F:\data-structure-agent\data-structure-agent\lesson-materials\lessons\00-lesson-index.md`；细分规划在相邻的 `curriculum.json`；OCR 审计和修订记录也在该目录。

原教材是耿国华《数据结构：用 C 语言描述》第 2 版。可用扫描 PDF 位于 `D:\Downloads\数据结构：用C语言描述  第2版【耿国华】高等教育出版社2015.7-9787040433050_ (2).pdf`；先核对 SHA-256 `51b0063103ce235554c4194708a3a73e9aa4241d26577d87033e841cc1371495`，不要只凭文件名信任。PDF 共 374 页，教材页 N 对应 PDF 页 N+11。此偏移在已有 362 个页块头部核对过，但每页内容仍需独立核验。

教材索引为 10 章、57 份原课时、正文 1–360 页（362 个页块，18 与 122 页重复）。细分课程规划有 95 个教学课时，全部可通过 `sourceLessonId` 归回 57 份原课时；两种粒度不可混用。当前数据库仅有 29 个既有核验页组成 8 个可选选段 / 24 个知识片段；其余 49 份原课时没有可用来源。线性表、栈与队列、图、内部排序、外部排序五章当前完全空白。Workbuddy 上一轮对照 PDF 新检查了 27 页（02-01 的 38–39，03-01 的 78–85，07-01 的 211–215，09-01 的 317–325，10-01 的 358–360），但这些页仅完成“识别 OCR 差异”，尚未逐字修订，更未导入。特别是图 214–215、栈的代码页、排序公式与代码、外排序题目序列严重损坏，禁止把现有 OCR 原样导入。

上轮接口未登录返回 401；“应返回 8 条”来自启动日志和导入规则复现，不是已登录 API 实测。你需要补做真实登录后的 API 和浏览器验收。当前 `replaceTextbook()` 固定写 `VERIFIED`，而导入器不读页级核验标签，是错误教材进入课堂的高风险入口；必须封住，不能只靠操作约定。

## 最终目标与执行授权

目标不是只把 8 条增加到 13 条，而是让教材第 1–10 章的 57 份原课时都具备真实、可追溯、可备课的来源，并让 `curriculum.json` 的 95 个细分课时都有明确映射和覆盖状态。若合理产品设计决定将细分课时作为课堂可选项，需保证后端身份、页范围、来源引用与前端展示一致；不能把一个原课时的部分核验页冒充为完整细分课时。用户已将这项本地工作完整委托给你：可在本机隔离测试数据库进行必要的受控导入和验证，不需每批再索要许可；不得操作生产数据或扩大到推送、部署。

你自行确定稳妥顺序，但必须完成下列结果：

1. 对 1–360 页逐页对照扫描 PDF。对照原文修订 OCR，尤其公式、C 代码、表格、图例及习题序列；图中文字无法准确转录时须保留可定位的图像证据或明确标注不可用，不可猜测。每页记录教材页、PDF 页、原文件 SHA、修订记录、审核状态。重新抽查既有 29 页，发现错误同样修订；只改 `OCR质量` 标签不算完成。
2. 将修订后的教材存入 Git 忽略的私有目录，维护机器可验证的页级清单。原 PDF 与完整教材原文不得写入 Git 跟踪文件、日志、提示词或测试快照。共享的第 18、122 页要明确唯一归属或去重策略，同时不损害两个课时的教学覆盖。
3. 修复导入安全：未核验、散列不匹配、页码/页数不符、缺少可追溯证据的材料必须拒绝导入或保持不可课堂使用；不能开启全量自动发布来绕过校验。新增测试覆盖失败路径及通过路径。必要时修正课时 ID 跨章冲突、页码标签聚合等问题，但不要做与课时覆盖无关的重构。
4. 在本机独立测试数据库执行可回滚、可重复的受控导入，保留既有用户与会话，不清空无关数据。核对数据库、已登录 API 和前端：可选课时数、章节、标题、页范围与清单一致；每个已发布课时都能依据自己的已核验片段备课。若模型/额度或个别过长课时导致失败，应修复或给出明确、可复现的限制，不能静默跳过。
5. 回归登录、Admin、课堂手动推进、插问恢复、判答、刷新恢复和动画，不因教材扩充破坏现有最简界面。新增课时至少覆盖十章各一例进行真实备课和浏览器抽查；全文覆盖应由自动化清单与接口检查证明。动画只承诺已有模拟器支持的操作，不支持的算法明确提示，不用无关动画冒充。
6. 更新 `docs/project/00-current-status.md` 与覆盖审计，明确已完成、未完成、证据、测试和剩余风险。交付给用户实际数字：57 份原课时/95 个细分课时各有多少完全覆盖、部分覆盖、未覆盖；列出本地 API 实测数量及失败项。只有确实满足全书证据和验收标准时，才能说“课程已全面”。

## 边界与完成标准

- 不接入 PPT、参考答案、教材配套源码、外部网页或模型常识作为课堂事实来源；它们只能辅助核对，不能代替原教材 PDF。
- 不泄露凭证；不把 PDF、完整教材正文、私有数据库放进跟踪目录；不推送、不部署；不删除或重置既有工作树修改。执行任何可能覆盖数据库的导入前先核对目标是本机隔离测试库并制作可恢复备份。
- 测试至少覆盖导入安全、课时数量和页范围、备课与会话、前端类型检查/构建及原有相关测试。实际浏览器验收要与单元测试区分记录。
- 如果全书逐页核验在当前执行环境不能真实完成，请完成所有安全可做的部分，清楚报告剩余页/课时和阻塞原因；绝不以 OCR 标签、模型自评或估算代替人工对照。不要再次只交付一个“建议先导入 27 页”的阶段性提示词。
